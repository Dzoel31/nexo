services:
  nexo-bot:
    image: ksmaiotupnvj/nexo-bot:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nexo_bot
    restart: always
    ports:
      - "8002:8000"
    environment:
      - DISCORD_BOT_TOKEN
      - PORT
      - GITHUB_SECRET
      - WEBHOOK_DEVLOGS_CHANNEL_ID
      - WEBHOOK_RELEASE_NOTES_CHANNEL_ID
      - WELCOME_AND_RULES_CHANNEL_ID
      - LLAMA_SERVER_URL
      - MCP_SERVER_URL
    volumes:
      - ./prompts:/app/prompts
      - ./data:/app/data

    deploy:
      resources:
        limits:
          memory: 300M
    networks:
      - hydroponic_network

networks:
  hydroponic_network:
    external: true
