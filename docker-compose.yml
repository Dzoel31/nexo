services:
  nexo-bot:
    image: ksmaiotupnvj/nexo-bot:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nexo_bot
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env

    volumes:
      - ./prompts:/app/prompts

    deploy:
      resources:
        limits:
          memory: 300M
    networks:
      - hydroponic_network

networks:
  hydroponic_network:
    external: true
